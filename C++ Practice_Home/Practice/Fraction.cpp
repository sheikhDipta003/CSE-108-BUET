# include <iostream>
# include <cstring>
using namespace std;

class Fraction{
public:
    int num;
    int denum;
    void printFraction(){   cout << num << " / " << denum;  }
    Fraction addFraction(Fraction a);
};

Fraction Fraction::addFraction(Fraction a){

}

int main(){

    return 0;
}